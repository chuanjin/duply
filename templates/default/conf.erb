# The GPG key to use
GPG_PW="<%= @gpg_pw %>"

# The target where to store the backup, e.g. S3
TARGET="<%= @target %>"
TARGET_USER="<%= @target_user %>"
TARGET_PASS="<%= @target_pass %>"

# base directory to backup
SOURCE="<%= @source %>"